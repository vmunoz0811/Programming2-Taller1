/**
 * Package that contains the methods made in model to interact with the user and return information
 */
package Controller;

/**
 * Main class that contain the method that execute the controller class
 */
public class AplMain {
    /**
     * Method that execute the controller class
     * @param args Parameter of main
     */
    public static void main(String[] args) {
        Controller c = new Controller();
    }
}
